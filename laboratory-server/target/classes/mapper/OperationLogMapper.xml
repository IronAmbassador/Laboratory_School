<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laboratory.mapper.OperationLogMapper">

    <insert id="insertOperationLog" parameterType="com.laboratory.model.entity.OperationLog">
        INSERT INTO operation_log (
            username,
            operation,
            operation_module,
            operation_time,
            operation_ip,
            operation_addr,
            operation_state,
            operation_result
        )
        VALUES (
                   #{username},
                   #{operation},
                   #{operationModule},
                   #{operationTime},
                   #{operationIp},
                   #{operationAddr},
                   #{operationState},
                   #{operationResult}
               )
    </insert>
</mapper>
